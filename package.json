{
  "name": "draw-guess-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root automation for Draw & Guess AI",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently --kill-others \"npm start --prefix backend\" \"npm run serve --prefix frontend\"",
    "backend": "npm start --prefix backend",
    "frontend": "npm run serve --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
